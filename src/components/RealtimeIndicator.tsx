import React from 'react';

const RealtimeIndicator: React.FC = () => {
  // No UI needed - SSE works silently in the background with auto-refresh
  return null;
};

export default RealtimeIndicator;
